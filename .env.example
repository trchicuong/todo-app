VITE_AI_API_KEY=your_google_generative_language_api_key
VITE_AI_MODEL=models/gemini-2.0-flash
VITE_VAPID_PUBLIC_KEY=<publicKey_base64url>
VITE_PUSH_SERVER_URL=<https://your-push-server>
 
# Server-side only (set in Netlify env, not exposed to client)
# Optional: Hugging Face token to increase free tier rate limits for voice transcription
# Leave empty to use public access (lower rate limit but still free)
# HUGGINGFACE_TOKEN=